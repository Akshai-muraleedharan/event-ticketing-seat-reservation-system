export enum BookingStatus {
    HELD = "HELD",
    CONFIRMED = "CONFIRMED",
    CANCELLED = "CANCELLED"
}

export enum BookingType {
    SEAT = "SEAT",
    PASS = "PASS"
}

export enum PaymentMode {
    MOCK = "MOCK",
    UPI = "UPI",
    CARD = "CARD"
}

export enum PaymentStatus {
    SUCCESS = "SUCCESS",
    FAILED = "FAILED"
}
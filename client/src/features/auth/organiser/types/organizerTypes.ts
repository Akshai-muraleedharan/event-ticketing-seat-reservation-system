export type LoginInputs = {
    email: string,
    password: string
}

export type RegisterInput = {
    fullName: string,
    email: string,
    phoneNumber: string,
    password: string
}

export type otpInput = {
    otp: string
}